## What & why

<!-- Summary of the change and the problem it solves. Link the issue. -->

## Checklist

- [ ] `npm run lint && npx tsc --noEmit && npm test && npm run build` pass
- [ ] **Spec updated**: behaviour changes update the relevant `spec/` file in
      this same PR (a spec that contradicts the code is a bug — see
      `spec/README.md`)
- [ ] New tables enable RLS; new migrations follow `NNN_snake_case.sql`
- [ ] Commits are signed off (DCO — see `CONTRIBUTING.md`)
- [ ] Commit messages and this PR title follow Conventional Commits
- [ ] No secrets, keys, or personal data in code, fixtures, or logs
