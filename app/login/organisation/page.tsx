import { LoginCard } from '@/components/LoginCard'

// Organisation members: sign in with a work email, or join a programme by code.
export default function OrganisationLoginPage() {
  return <LoginCard variant="organisation" />
}
